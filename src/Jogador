#ifndef PLAYER_HEADER //Linha de Pré-processamento: O compilador verifica se PLAYER_HEADER já foi definido anteriormente, caso tenha sido definido, todo esse arquivo será ignorado, se não foi definido então segue o bloco de código abaixo\/
#define PLAYER_HEADER // Se o PLAYER_HEADER foi definido, o compilador entende que este identificador já foi usado e define como: definido
// REVISA ELES E SEGUE:

class Player {
public:
    int x, y; // Posição do jogador
    int score; // Pontos do jogador

    Player(int startX, int startY) : x(startX), y(startY), score(0) {} // Construtor

    void moveUp() { if (y > 0) y--; } // Mover para cima
    void moveDown() { if (y < 11) y++; } // Mover para baixo
};

#endif // PLAYER_HEADER

